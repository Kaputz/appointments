{% extends 'vang_base.html.twig' %}

{% block app_title %}
    {% block title %} Vanguarda {% endblock %} - Marcação Encomendas Cais Recepção
{% endblock %}

{% block logo %}{{ asset('logo.png') }}{% endblock %}
{% block favicon %}{{ asset('favicon.png') }}{% endblock %}

{% block head_js %}
    {{ parent() }}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <!--<link href="https://fonts.googleapis.com/css?family=Fira Mono" rel="stylesheet">-->
{% endblock %}

{% block header_navigation_links %}

    {% if is_granted('ROLE_ADMIN') %}
        <li>
            <a href="{{ path('user') }}">
                <i class="fa fa-user" aria-hidden="true"></i> {{ 'menu.users'|trans }}
            </a>
        </li>
    {% endif %}

    <li>
        <a href="{{ path('appointment') }}">
            <i class="fa fa-calendar" aria-hidden="true"></i> {{ 'menu.appointments'|trans }}
        </a>
    </li>
    
    {# 
    <li>
        <a href="{{ path('appointment_list') }}">
            <i class="fa fa-home" aria-hidden="true"></i> {{ 'menu.appointment_list'|trans }}
        </a>
    </li> 
    #}

    {# Exemplo de menu apenas para admins. #}
    {# {% if is_granted('ROLE_ADMIN') %}
        <li>
            <a href="{{ path('homepage') }}">
                <i class="fa fa-lock" aria-hidden="true"></i> {{ 'menu.admin'|trans }}
            </a>
        </li>
    {% endif %} #}

{% endblock %}